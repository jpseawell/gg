# scripts

Place reusable shell or node scripts here once deployment or local orchestration grows beyond simple package scripts.

- `clean.mjs` removes generated build, coverage, and browser-test artifacts.
- `rename-template.mjs` updates starter package names and common visible references after creating a new project from the template.
