# netlify

Keep Netlify-specific supporting notes or helper scripts here if Netlify becomes a real deploy target. Store `netlify.toml` at the repo root only when you actually need the platform entrypoint.
